import 'package:flutter/material.dart';

class TelaDetalhesPedido extends StatelessWidget {
  const TelaDetalhesPedido({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}